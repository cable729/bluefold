/// SearchIndexKit builds and queries the SQLite FTS5 full-text index over
/// PDF page text, keyed by content hash so indexes survive file moves/renames.
public enum SearchIndexKitInfo {
    public static let moduleName = "SearchIndexKit"
}
