/// CalibreKit provides strictly read-only access to a Calibre library:
/// it copies metadata.db aside before reading and never writes to the library.
public enum CalibreKitInfo {
    public static let moduleName = "CalibreKit"
}
